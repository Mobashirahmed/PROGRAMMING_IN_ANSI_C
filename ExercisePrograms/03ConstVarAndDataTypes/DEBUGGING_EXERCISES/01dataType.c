void main()
{
    // Int x;
    int x;
    float letter, DIGIT;
    double p,q;
    // exponent alpha, beta;
    // m, n, z : INTEGER
    int m, n, z;
    // short char c;
    char c;
    // long int m; count;
    long int o, count;
    // long float temp;
    double temp;
}

/* errors:
    1. enclosing the variable declarations inside a main function was left
    2. data type keywords must be in lower case, the first letter of int was capitalised
    3. every line of statements inside the main function should end with a semi-colon
    4. in C there is no data type named exponent
    5. syntax erorr in declaring three variables of integer data type each variable must be seperated by comma operator not semi-colon.
    6. invalid combination of type specifiers 'shor char' and 'long float' */