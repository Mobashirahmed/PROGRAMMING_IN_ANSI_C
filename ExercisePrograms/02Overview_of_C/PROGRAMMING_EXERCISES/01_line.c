/* Write a program to display the equation of line in the form
                                            ax + by = c
    for a=5, b=8 and c=18. */
#include<stdio.h>

int main(){
    int a=5, b=8, c=18;
    printf("Equation of line: %dx + %dy = %d", a, b, c);
    return 0;
}